services:
  backend:
    build:
      context: .
      dockerfile: docker/dev/backend-dev.Dockerfile
    container_name: fastapi_backend
    ports:
      - "8000:8000"
      - "5678:5678"
    volumes:
      - ./back-end:/app
      - ./worker:/app/worker
    environment:
      - MONGODB_URI=mongodb://mongo:27017/app_db
      - REDIS_URL=redis://redis:6379/0
      - CELERY_BROKER_URL=amqp://guest:guest@rabbitmq:5672//
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
    depends_on:
      - mongo
      - redis
      - rabbitmq
  
  worker:
    build:
      context: .
      dockerfile: docker/worker.Dockerfile
    container_name: fastapi_worker
    volumes:
      - ./back-end:/app/back-end
      - ./worker:/app/worker
    environment:
      - MONGODB_URI=mongodb://mongo:27017/app_db
      - REDIS_URL=redis://redis:6379/0
      - CELERY_BROKER_URL=amqp://guest:guest@rabbitmq:5672//
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
    depends_on:
      - mongo
      - redis
      - rabbitmq

  flower:
    image: "mher/flower:latest"
    container_name: fastapi_flower
    ports:
      - "5555:5555"
    environment:
      - CELERY_BROKER_URL=amqp://guest:guest@rabbitmq:5672//
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
    command: celery flower --port=5555 --address=0.0.0.0 --basic_auth=admin:admin123
    depends_on:
      - rabbitmq
      - redis

  frontend:
    image: node:22
    container_name: vite_frontend
    working_dir: /app
    ports:
      - "5173:5173"
    volumes:
      - ./front-end:/app
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    depends_on:
      - backend

  mongo:
    image: mongo:6
    container_name: mongo_db
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  redis:
    image: redis:7
    container_name: redis_cache
    ports:
      - "6379:6379"

  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq_broker
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      - RABBITMQ_DEFAULT_USER=guest
      - RABBITMQ_DEFAULT_PASS=guest

volumes:
  mongo_data:
